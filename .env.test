# OpenCap Test Environment Variables

# MongoDB Configuration
MONGODB_URI=mongodb://opencap:password123@localhost:27018/opencap_test?authSource=admin
MONGODB_VERSION=7.0.3

# PostgreSQL Configuration
POSTGRES_URI=postgresql://postgres:password@localhost:5433/opencap_test

# Server Configuration
PORT=3001
NODE_ENV=test

# Authentication
JWT_SECRET=test_jwt_secret
JWT_EXPIRATION=1h

# Shortcut API Configuration (not needed for tests)
SHORTCUT_API_TOKEN=test_token

# Email Configuration (mocked in tests)
EMAIL_SERVICE=ethereal
EMAIL_API_KEY=test_key

# Logging
LOG_LEVEL=error
