export const metadata = {
  title: 'Create Account',
  description:
    'Create your OpenCap Stack account. Get started with open-source cap table management for your startup.',
  alternates: { canonical: 'https://opencapstack.com/register' },
};

export default function RegisterLayout({ children }) {
  return children;
}
