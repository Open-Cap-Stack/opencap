export const metadata = {
  title: 'Settings',
  description: 'Manage your OpenCap Stack account settings, company profile, and integrations.',
  robots: { index: false, follow: false },
  alternates: { canonical: 'https://opencapstack.com/settings' },
};

export default function SettingsLayout({ children }) {
  return children;
}
